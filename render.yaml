services:
  - type: web
    name: server1port
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    autoDeployTrigger: commit
    envVars:
      - key: AUTH_KEY
        sync: false
      - key: PORT
        value: "10000"
